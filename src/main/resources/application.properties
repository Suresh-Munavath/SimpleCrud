spring.application.name=springbootbackend

# ? Updated JDBC URL with timezone
spring.datasource.url=jdbc:mysql://localhost:3306/ems?useSSL=false&allowPublicKeyRetrieval=true

server.port=8081

spring.datasource.username=root
spring.datasource.password=root

# ? Use the correct Hibernate dialect for MySQL 8
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update

# ? (Optional but useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
